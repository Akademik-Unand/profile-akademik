services:
  backend-profile-akademik:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: backend-profile-akademik:latest
    container_name: backend-profile-akademik
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env
    environment:
      NODE_ENV: production
    volumes:
      - ./backend/uploads:/app/uploads
      - ./backend/logs:/app/logs
    networks:
      - network
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--quiet",
          "--tries=1",
          "--spider",
          "http://localhost:3001/health",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  frontend-profile-akademik:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: /api/v1
    image: frontend-profile-akademik:latest
    container_name: frontend-profile-akademik
    restart: unless-stopped
    ports:
      - "5174:80"
    depends_on:
      backend-profile-akademik:
        condition: service_healthy
    networks:
      - network
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--quiet",
          "--tries=1",
          "--spider",
          "http://localhost:80",
        ]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  network:
    driver: bridge
